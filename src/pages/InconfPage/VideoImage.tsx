import Box from "@mui/material/Box";

const VideoImage = () => {
  return (
    <Box
      sx={{
        backgroundColor: "#000",
        minWidth: 500,
        height: 250,
        mx: "auto",
        borderRadius: 3
      }}
    ></Box>
  );
};

export default VideoImage;
